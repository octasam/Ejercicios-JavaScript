/** Ejercicio para esta sesion
 * 
 * Declarar 10 variables de cada tipo de dato sin tomar en cuenta objetos, arrays y null
 * Pueden variar entre las palabras reservadas var, let y const
 *      -number
 *      -string
 *      -boolean
 * 
 * Declarar variables numericas y realizar las operaciones aritmeticas basicas
 *      -suma
 *      -resta
 *      -multiplicacion
 *      -division
 * 
 * Declarar variables que concatenen cadenas de texto
 *      -5 mensajes diferentes en pantalla, sin limite de palabras.
 * 
 * Declarar 5 variables y convertirlas a otro tipo de dato
 * 
 *  CONSIDERACIONES: poner en practica las mejores practicas para declaracion de variables.
 * Imprimir los resultados en pantalla con un console.log()
 */

var namePet = 'Rony';
var agePet = 3;
var breedPet = 'Basenji';
var isBarkless = true;
var colour = 'Red and white'
var heightInches = 17;
var weightPounds = 24;
var isMale = true;
var nationality = 'Africa';


//2da parte

var pi = pi;
var year = 2023;
var month = 9;
var day = 18;

var sum = year + month;
var substract = year - month - day;
var multiplication = year * month * day;
var disivion = year / month;

//3ra parte

var city = 'CDMX';
var country = 'Mexico';
var continente = 'America';

var infoLocation = city + country;

//4ta parte

var newAge = String(agePet);
var newName = Boolean(namePet);
var newHeight = String(heightInches);
var newName = Number(namePet);